"use client";

import AppShell from "@/app/components/AppShell";
import { SupportWorkspace } from "@/app/workspace/pages/SupportWorkspace";

export default function TicketsClient() {
  return (
    <AppShell>
      <SupportWorkspace />
    </AppShell>
  );
}
