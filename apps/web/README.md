# 6esk Web

This workspace is the target home for the customer/operator product app.

During the backoffice split, the production web app still lives at the repo root
so ongoing product changes are not moved over unrelated in-flight edits. The
workspace scripts delegate to the current root app until the product app move can
be completed as its own mechanical migration.
